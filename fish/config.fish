source ~/.bash_profile

# project find
function pf --description 'Finds a project folder'
    # find is one of my own functions for changing into the project file
    cd (projectfolderfind)
    clear
    ls
end

alias vi='nvim'

